# BitArray
Реализация BitArray через вектор

## run

### build
```bash
cmake CMakeLists.txt -B build
cd build
make all
```

### run test
```bash
# in build dir
./tests/lab1_tests
```
### run program
(add some code in main.cpp for use it)
```bash
# in build dir
./src/lab1_run
```
